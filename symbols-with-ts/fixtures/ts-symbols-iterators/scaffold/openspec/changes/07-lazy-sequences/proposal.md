## Why

Shows the practical payoff of lazy iterables — composable, memory-efficient pipelines over infinite or large sequences.

## What Teaches

Infinite generators, `take(n)`, lazy `map`/`filter` adapters, pipeline composition without intermediate arrays.

## Prereqs

05-generator-functions

## Success criterion

The test suite in `lessons/07-lazy-sequences/lazy.test.ts` passes.
