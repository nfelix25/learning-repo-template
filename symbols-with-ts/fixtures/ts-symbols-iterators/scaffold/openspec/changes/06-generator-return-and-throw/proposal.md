## Why

Covers the bidirectional communication paths in generators — essential for understanding resource cleanup, which is a core theme of this project.

## What Teaches

`gen.return()`, `gen.throw()`, `try/finally` in generators, cleanup on early exit.

## Prereqs

05-generator-functions

## Success criterion

The test suite in `lessons/06-generator-return-and-throw/generator-cleanup.test.ts` passes.
