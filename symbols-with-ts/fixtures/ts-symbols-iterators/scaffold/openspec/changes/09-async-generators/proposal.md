## Why

Async generators are the idiomatic way to produce async iterables. Prereq for both the Streams lesson and the async pipeline build piece.

## What Teaches

`async function*`, interleaving `await` and `yield`, error propagation in async generators.

## Prereqs

08-async-iterator-protocol, 06-generator-return-and-throw

## Success criterion

The test suite in `lessons/09-async-generators/async-generators.test.ts` passes.
