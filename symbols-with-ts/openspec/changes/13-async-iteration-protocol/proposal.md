## Why

Async iteration extends the protocol idea to values that arrive over time.

## What Teaches

This lesson teaches Symbol.asyncIterator, async iterables, for-await-of, async protocol fallback boundaries.

## Prereqs

`12-iteration-protocol`

## Success criterion

The test suite in `lessons/13-async-iteration-protocol/symbols.test.ts` passes.
