## Why

TypeScript can make symbol-heavy JavaScript easier to state, but the runtime semantics stay JavaScript-first.

## What Teaches

This lesson teaches symbol type, unique symbol, symbol-keyed interfaces, PropertyKey, typing well-known symbol methods.

## Prereqs

`08-symbols-as-api-tokens`, `12-iteration-protocol`

## Success criterion

The test suite in `lessons/22-typescript-notation-for-symbol-heavy-js/symbols.test.ts` passes.
