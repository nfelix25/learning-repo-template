## Why

Copying and serialization are where symbol visibility becomes operational instead of theoretical.

## What Teaches

This lesson teaches Object.assign with symbol keys, object spread with symbol keys, enumerable own symbol properties, JSON serialization boundaries, debugging symbol-bearing objects.

## Prereqs

`05-enumeration-reflection-and-visibility`

## Success criterion

The test suite in `lessons/06-copying-spread-json-and-serialization/symbols.test.ts` passes.
