## Why

Move from the primitive value to the object model: symbols matter because they are one of the valid property-key domains.

## What Teaches

This lesson teaches ECMAScript PropertyKey model, string keys vs symbol keys, object property records, key coercion boundaries.

## Prereqs

`01-symbol-primitive-identity`

## Success criterion

The test suite in `lessons/02-property-keys-and-object-internals/symbols.test.ts` passes.
