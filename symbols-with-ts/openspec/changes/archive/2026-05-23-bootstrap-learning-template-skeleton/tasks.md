## 1. Project Baseline

- [x] 1.1 Add `package.json` with TypeScript/Vitest dev dependencies and scripts for `typecheck`, `test`, and `verify`.
- [x] 1.2 Add `tsconfig.json` with strict TypeScript settings, including `noUncheckedIndexedAccess`.
- [x] 1.3 Add `vitest.config.ts` for the repository test runner.
- [x] 1.4 Ensure the package configuration does not add `tsd` or a `tsd` workflow.

## 2. Documentation

- [x] 2.1 Add `README.md` describing the repository purpose, relationship to `HANDOFF.md`, and skeleton milestone scope.
- [x] 2.2 Add the agreed OpenSpec change roadmap to `README.md`.
- [x] 2.3 Add `STYLE.md` with initial conventions for Vitest `expectTypeOf`, koan format, content style, and TypeScript compiler expectations.

## 3. Skill Compatibility Layout

- [x] 3.1 Add trackable placeholder directories for future learning-template skills under `.codex/skills/`.
- [x] 3.2 Add matching trackable placeholder directories for future learning-template skills under `.claude/skills/`.
- [x] 3.3 Confirm placeholders do not implement functional `SKILL.md` behavior for the new learning-template skills.

## 4. Preservation And Verification

- [x] 4.1 Confirm existing OpenSpec workflow commands, skills, and `openspec/config.yaml` remain present.
- [x] 4.2 Verify the skeleton configuration with the available package scripts after dependencies are available.
- [x] 4.3 Review changed files against `learning-template-skeleton` requirements before marking the change complete.
