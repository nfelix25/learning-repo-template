# 06: Typed Methods

Decorators are runtime functions, but their TypeScript signatures still matter. This koan constrains a decorator so it can only wrap methods that return `number`, and so its replacement must also return `number`.

Run it with:

```sh
npm run test:koan -- koans/06-typed-methods/exercise.test.ts
```
