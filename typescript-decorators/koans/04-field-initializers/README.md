# 04: Field Initializers

Modern field decorators can return an initializer function. That initializer receives the field's initial value for each instance and can return a replacement value.

Run it with:

```sh
npm run test:koan -- koans/04-field-initializers/exercise.test.ts
```
