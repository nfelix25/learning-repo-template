# 02: Method Wrapper

Modern method decorators can return a replacement function. This koan wraps a method while preserving `this`, arguments, and the original return value.

Run it with:

```sh
npm run test:koan -- koans/02-method-wrapper/exercise.test.ts
```
