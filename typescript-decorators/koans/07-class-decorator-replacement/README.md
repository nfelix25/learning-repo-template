# 07: Class Decorator Replacement

Modern class decorators can return a replacement constructor. This koan returns a subclass that preserves the original constructor behavior and adds a runtime tag.

Run it with:

```sh
npm run test:koan -- koans/07-class-decorator-replacement/exercise.test.ts
```
