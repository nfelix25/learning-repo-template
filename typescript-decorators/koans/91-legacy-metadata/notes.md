# Notes

- What ran at decoration time?
- What metadata did TypeScript emit because `emitDecoratorMetadata` was enabled?
- Why does this require the legacy compiler mode instead of modern decorators?
- Which facts did TypeScript know statically, and which facts were only visible at runtime?
