# 90: Legacy Orientation

Legacy experimental decorators are a different compiler mode. They use target/property/descriptor arguments, not modern value/context arguments.

This koan requires `tsconfig.legacy.json`, which enables `experimentalDecorators`.

Run it with:

```sh
npm run test:legacy
```
