export function hasUndirectedCycle(graph: number[][]): boolean {
  // Returns true if the undirected graph (given as adjacency list) contains a cycle
  throw new Error('TODO');
}

export function hasDirectedCycle(graph: number[][]): boolean {
  // Returns true if the directed graph (given as adjacency list) contains a cycle
  throw new Error('TODO');
}

export function findCycleVertices(graph: number[][]): number[] | null {
  // For directed graph: returns one cycle as array of vertex indices, or null if no cycle
  throw new Error('TODO');
}
