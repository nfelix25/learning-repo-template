## 1. Implementation

- [x] 1.1 Write `lessons/18-react-type-patterns/lesson.md` (Motivation, Mechanic, Worked example, Pitfalls, Exercise)
  - Add a React version block at the top: cover React 18 and React 19 patterns separately, clearly labeled
  - Cite the React 19 release notes for `forwardRef` deprecation
  - Cite the React `Using TypeScript` docs for current recommended patterns
- [x] 1.2 Write `lessons/18-react-type-patterns/react-types.test.ts` (failing until workspace is complete)
  - Tests should be version-agnostic where possible; use conditional compilation comments where not
- [x] 1.3 Write `lessons/18-react-type-patterns/react-types.ts` workspace stub with `// TODO` markers
